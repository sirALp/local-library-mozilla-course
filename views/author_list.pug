extends layout

block content
  h1= title

  if author_list.length
    ul
      each author in author_list
        li
          a(href=author.url) #{author.name}   
          if author.date_of_birth
            span(style="font-size:.85em;color:darkgreen") (#{author.lifespan})
          else
            span(style="font-size:.85em;color:darkred;") (Unknown)
  else
    p There are no authors.
